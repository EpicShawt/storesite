const express = require('express');
const router = express.Router();

// Placeholder for orders functionality
router.get('/', (req, res) => {
  res.json({ message: 'Orders functionality coming soon' });
});

module.exports = router; 